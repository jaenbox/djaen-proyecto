{# app/Resources/views/Cliente/show.html.twig #}
{% extends "base.html.twig" %}
{% trans_default_domain 'AppBundle' %}

{% block title %} Cliente-show {% endblock %}
{% block stylesheets %}
	{{ parent() }}
{% endblock %}

{% block body %}
	<div class="container contenedor">
		<div class="row">   
       		<div class="col-xs-12">
       			<div class="col-xs-12 col-sm-12">
    				<p style="padding:10px;"></p>
    			</div>
				<ol class="breadcrumb">
		        	<li><a href="{{ path('index') }}">inicio</a></li>
		        	<li><a href="{{ path('user') }}">cliente</a></li>
		            <li>show</li>
		       	</ol>
		    </div>
		</div>
		<h2>Cliente: {{ cliente.name }}</h2>
		<hr>
		<a href="{{ path('user') }}"><button type="button" class="btn btn-default">Atras</button></a>	
		<hr>
		<table class="table">
			<tbody>
				<tr><th><i class="fa fa-user fa-2x"></th><td>{{ cliente.id }}</td></tr>
				<tr><th>Nombre</th><td>{{ cliente.name }}</td></tr>
				<tr><th>Apellidos</th><td>{{ cliente.surname }}</td></tr>
				<tr><th>DNI</th><td>{{ cliente.dni }}</td></tr>
				<tr><th><i class="fa fa-at fa-2x"></th><td>{{ cliente.email }}</td></tr>
				<tr><th><i class="fa fa-phone-square fa-2x"></th><td>{{ cliente.phone }}</td></tr>					
				<tr><th>Dirección</th><td>{{ cliente.address }}</td></tr>
				<tr><th>Administrador</th><td>{{ cliente.administrador.name }}</td></tr>
			</tbody>
		</table> 
		<hr>
	</div>
	
{% endblock %}
{% block javascripts %}{% endblock %}
